#include "main.h"

void positive_or_negative(int i)
{
	if (i > 0)
		printf("%d i positive \n", i);

	else if (i < 0)
		printf("%d i negative \n", i);
	else 
                printf("%d i zero \n", i);
}
